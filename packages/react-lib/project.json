{
  "name": "@telicent-oss/react-lib",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "yarn test"
          }
        ],
        "cwd": "packages/react-lib",
        "parallel": false
      }
    }
  }
}