{
  "name": "@telicent-oss/catalogservice",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "yarn test"
          }
        ],
        "cwd": "packages/catalogservice",
        "parallel": false
      }
    },
    "printhello": {
      "executor": "nx:run-commands",
      "outputs": [],
      "options": {
        "command": "echo hello"
      }
    }
  }
}