{
  "name": "@telicent-oss/eslint-plugin-ds",
  "version": "0.0.1",
  "private": true,
  "//private": "private until we get an npmjs.org package created",
  "license": "Apache-2.0",
  "description": "ESLint rules enforcing the Telicent design system manifest: Tailwind may do layout, size and spacing, and nothing else.",
  "type": "module",
  "//main": "the esm entry: this package is esm only, see vite.config.ts",
  "main": "./dist/eslint-plugin-ds.es.js",
  "module": "./dist/eslint-plugin-ds.es.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "//types": "first: conditions resolve in order, and only dist/index.d.ts is emitted",
      "types": "./dist/index.d.ts",
      "import": "./dist/eslint-plugin-ds.es.js"
    }
  },
  "files": [
    "dist/*"
  ],
  "scripts": {
    "test": "jest --runInBand",
    "coverage": "jest --runInBand --coverage",
    "lint": "eslint \"src/**/*.ts\"",
    "tscNoEmit": "tsc --noEmit",
    "build": "rm -rf ./dist && vite build",
    "//prepare": "consumers install this from git, where only prepare runs; without it they would get no dist. Inlined rather than calling yarn build: the consumer may be on pnpm or npm",
    "prepare": "rm -rf ./dist && vite build",
    "prepublish": "yarn lint && yarn test",
    "prepublishOnly": "yarn build"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "//peerDependencies": "the rule's visitor API is identical in 8 and 9; consumers on flat config run 9",
  "peerDependencies": {
    "eslint": ">=8"
  },
  "devDependencies": {
    "@types/eslint": "^8.56.0",
    "@types/jest": "^29.5.12",
    "@types/node": "18.19.9",
    "@typescript-eslint/parser": "^6.20.0",
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.3",
    "typescript": "^5.5.4",
    "vite": "^4.5.3",
    "vite-plugin-dts": "^3.6.3"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/telicent-oss/rdf-libraries.git",
    "directory": "packages/eslint-plugin-ds"
  }
}
