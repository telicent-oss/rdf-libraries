#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

./scripts/cp-LICENSE-root-to-packages.sh
./scripts/check-release-please-config/index.js
yarn test

# Two packages commit their build output, because they are consumed from a git pin and
# nothing compiles them on the way in. Committed output can go stale, so it is rebuilt
# and compared here. Only those two define the script.
yarn lerna run verify-dist
