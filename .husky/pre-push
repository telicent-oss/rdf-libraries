#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

./scripts/cp-LICENSE-root-to-packages.sh
./scripts/check-release-please-config/index.js
yarn test

# Two packages commit their dist, because a git pin installs no build. Rebuild and
# compare, so it cannot go stale.
yarn lerna run verify-dist
